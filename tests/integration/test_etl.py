import myfitnesspaw

# Test where a user uses default configuration to extract 5 days not available in db

# Test where a user uses default confuguration to extract 5 days already in db (with changes)

# Test where a user passes parameters to extract 1 day already in db (with changes)

# Test where a user passes parameters to extract 1 day already in db (to delete available info)
